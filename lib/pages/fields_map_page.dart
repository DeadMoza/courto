import 'package:flutter/material.dart';

class FieldsMapPage extends StatelessWidget {
  const FieldsMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("خريطة الملاعب"),
    );
  }
}
